apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: standup-counter

resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Uncomment to set a custom image
# images:
#   - name: standup-counter
#     newName: your-registry/standup-counter
#     newTag: v1.0.0
