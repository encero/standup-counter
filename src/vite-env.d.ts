/// <reference types="vite/client" />

// Injected by Vite at build time (see vite.config.ts). Identifies the client
// build so it can be compared against the server's running version.
declare const __APP_VERSION__: string;
